import 'package:flutter/material.dart';

class AppColors {
  // Light mode colors
  // static const Color lightBackground = Color(0xFFF9F9F9);
  static const Color lightBackground = Color.fromRGBO(240, 240, 240, 1);
  static const Color lightBackground2 = Colors.white;

  static const Color lightTitle = Color(0xFF222222);
  static const Color lightSubtitle = Color(0xFF666666);

  // Dark mode colors
  // static const Color darkBackground = Color(0xFF121212);
  static const Color darkBackground = Color.fromRGBO(25, 25, 25, 1);

  static const Color darkTitle = Color(0xFFFFFFFF);
  static const Color darkSubtitle = Color(0xFFAAAAAA);

  static const Color darkBackground2 = Colors.black;
}
